<div class="site-heading">
    @if(Context.String(BlogExKeys.Description) != null)
    {
        <h1>@Context.String(BlogExKeys.Description)</h1>
    }
    @if(Context.String(BlogExKeys.Intro) != null)
    {
        @if(Context.String(BlogExKeys.Description) != null)
        {
            <hr class="small">
        }
        <span class="subheading">@Context.String(BlogExKeys.Intro)</span>
    }
</div>
